package api.endpoints;

import api.core.Request;
import io.restassured.authentication.AuthenticationScheme;

public class InvoiceAPI extends Request {
    //TODO: Implement this at home

    public InvoiceAPI(String baseUri, String basePath, AuthenticationScheme scheme) {
        super(baseUri, basePath, scheme);
    }
}
